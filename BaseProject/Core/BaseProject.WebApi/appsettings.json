{
  "AppSettings": {
    "Token": "super secret key"
  },
  "ConnectionStrings": {
    "BaseProjectDatabase": "Server=.\\sqlexpress;Database=BaseProject;Trusted_Connection=True;Application Name=BaseProject;"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "JwtIssuerSettings": {
    "Issuer": "BaseProject",
    "Audience": "http://localhost:4200/"
  },
  "AuthSettings": {
    "SecretKey": "iNivDmHLpUA223sqsfhqGbMRdRj1PVkH"
  },
  "AllowedHosts": "*",
  "ResponseCompressionSettings": {
    // Additional MIME types (other than the built in defaults) to enable GZIP compression for.
    "MimeTypes": [
      // ATOM
      "application/atom+xml",
      // Images
      "image/svg+xml",
      "image/x-icon",
      // Fonts
      "application/vnd.ms-fontobject",
      "application/x-font-ttf",
      "font/otf"
    ]
  },
  "CacheProfileSettings": {
    "CacheProfiles": {
      // Cache the error route (/error/404/notfound) by status code for a day.
      "Error": {
        "Duration": 86400,
        "Location": "Any"
      },
      // Cache the robots.txt route for a day.
      "RobotsText": {
        "Duration": 86400,
        "Location": "Any"
      },
      // Cache static files for a year.
      "StaticFiles": {
        "Duration": 31536000,
        "Location": "Any"
      }
    }
  }
}
